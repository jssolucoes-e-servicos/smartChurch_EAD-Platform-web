/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* async redirects() {
    return [
      {
        source: "/app",
        destination: "/app/inicio",
        permanent: false,
      },
      {
        source: "/login",
        destination: "/",
        permanent: false,
      },
    ];
  }, */
};

module.exports = nextConfig;
